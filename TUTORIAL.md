# 拓径 PATHFORGE 使用教程

> 界面导览 · 典型场景 · 快捷键 · 常见问题
> 安装与启动见 [README.md](README.md)；接口契约见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)。

---

## 1. 界面导览

```
┌─ 顶栏：侧栏开关 · 标识 · 后端状态(provider/model) · 运行状态 · Token 面板 · 主题 · 右栏开关 ─┐
├────────────┬──────────────────────────────────────────────┬──────────────────────────────┤
│  左侧栏     │              中央对话 / 任务区                 │        右侧详情面板          │
│ 新建会话    │  消息流（用户右对齐 / Agent 左对齐 Markdown） │  文件 · 工具 · Token · Git   │
│ 会话列表    │  执行轨迹读条 + 节点详情                       │                              │
│(按工作区分组)│  实时指标（TPS / 上下文 / 缓存命中 / Token）  │  文件树与预览                │
│ 置顶/重命名/ │  输入区（@ 引用 · 上传 · 工作区 · 预设 ·      │  工具调用输入输出            │
│ 删除        │          权限 · 模型 · 推理强度）             │  Token 消耗与命中率          │
│ 设置        │                                              │  Git 变更 stage/unstage      │
└────────────┴──────────────────────────────────────────────┴──────────────────────────────┘
```

- **折叠**：顶栏左侧按钮折叠侧栏（`56px` Rail ↔ `280px` 完整）；顶栏右侧按钮收起详情面板。
- **响应式**：窗口宽度 < 1024px 时右侧面板自动关闭、左侧栏强制为 Rail。
- **主题**：顶栏太阳/月亮图标切换深/浅色，默认深色。

---

## 2. 五个典型场景

### 场景一：分析一份 JD 并匹配简历（最常用）

1. 先把简历放进工作区：点输入区左侧的**上传按钮**（或直接把文件拖进对话框）；
2. 在输入区输入 `@`，从浮层里选中简历文件，插入 `@路径`；
3. 同样方式 `@` 引用 JD 文件（`data/jds/` 下自带 10 份真实风格 JD 可直接用）；
4. 发送，例如：

   ```
   帮我分析 @data/jds/jd03_数据分析师.txt，匹配 @data/resumes/简历_李明_数据分析师.txt，给出面试题
   ```

5. 观察中央区下方的**轨迹读条**：`recall → plan → dispatch → react → synthesize` 逐条点亮；
   点击任意读条查看该节点的思考、工具入参/输出、耗时与 Token；
6. 报告生成后右侧面板「Token」查看本次消耗与缓存命中率，「工具」查看每次工具调用的明细。

### 场景二：只贴 JD 截图，不打字

直接 `Ctrl+V` 把招聘网站截图粘进输入框（或拖入图片），后端会走本地 OCR 识别文字并纳入分析，
无需手动录入 JD 正文。

### 场景三：信息不足时让 Agent 追问

只发一句 `帮我找份好工作`。Agent 不会瞎猜，而是通过 `ask_user` 挂起并在输入区上方显示追问；
补充「目标岗位 + 城市 + 现有简历」后再发送，图会从检查点续跑。

### 场景四：切换预设与权限

| 想要的效果 | 选择 |
|---|---|
| 完整分析 + 正式报告 | 预设 **标准** |
| 快速一问一答 | 预设 **极简** |
| 关键结论必须自检 | 预设 **PTC**（规划–工具–校验） |
| 发散型头脑风暴 | 预设 **创造** |
| 只让 Agent 读、不许改文件 | 权限 **只读** |
| 允许在工作区内创建/修改文件 | 权限 **工作区可写**（默认） |
| 允许越出工作区 | 权限 **完全访问**（需后端 `ALLOW_FULL_ACCESS=true`） |

**推理强度**（低/中/高）影响采样温度与 ReAct 步数上限：低=更快更稳，高=更发散更彻底。

### 场景五：中断、回滚与清理

- **中断**：执行中发送按钮变为停止按钮，点击即中断；已产出的部分照常保存；
- **回滚**：点击「回滚」把该会话的消息/任务/产物恢复到本次运行起点（**不会删除工作区里已生成的文件**）；
- **重命名 / 置顶 / 删除会话**：悬停左侧栏会话条目，右侧出现操作图标；
- **删除记忆**：在对话中发送 `删除我的所有记忆` 可清空画像与上下文（本地即时响应，不消耗模型调用）。

---

## 3. 设置（左下角齿轮）

| Tab | 内容 |
|---|---|
| **通用设置** | 主题、侧栏/右栏默认折叠状态、默认权限模式、后端依赖健康状态（数据库/Redis/向量库/沙箱） |
| **模型配置** | 可用模型列表与当前模型切换（模型清单由后端 `LLM_MODELS` 提供） |
| **插件管理** | 内置工具与自定义工具列表；注册 HTTP / MCP 兼容工具；卸载自定义工具（内置工具受保护） |
| **Agent 预设** | 四档预设的说明与参数，点击即切换 |

---

## 4. 快捷键

| 按键 | 作用 |
|---|---|
| `Enter` | 发送 |
| `Shift + Enter` | 换行 |
| `Ctrl/Cmd + V` | 粘贴截图 → 自动 OCR |
| `@` | 呼出工作区文件引用浮层（↑↓ 选择、Enter 确认、Esc 关闭） |
| `Esc` | 关闭浮层 / 设置弹窗 |

---

## 5. 常见问题

**Q：没有配置模型凭据能跑吗？**
能。后端会自动进入**离线 mock 模式**，用启发式模板驱动完整的规划→工具→报告链路，
工具产物（技能词频、匹配分、图表）依然来自真实计算。适合演示与自测。

**Q：界面上显示"后端未连接"怎么办？**
确认后端已启动（`uvicorn main:app`）且端口与前端代理一致（默认 `127.0.0.1:8000`）。
前端开发服务器已在 `vite.config.ts` 里代理 `/api` 与 `/ws`。

**Q：执行过程没有轨迹读条？**
轨迹来自 SSE 的 `node_start` / `node_end` / `trajectory` 事件。请检查浏览器网络面板中
`/api/agent/run` 是否为 `text/event-stream`，以及后端日志是否报错。

**Q：报告里的图表打不开？**
图表由 `resume_match` 工具生成并落盘 `data/reports/charts/`，通过 `/api/files/report` 白名单
路由提供（只放行该目录，其他路径一律 404——这是防止 `.env`、会话存档被下载的安全设计）。

**Q：Token 统计显示 `—`？**
离线 mock 模式下 Token 是按字符数估算的；真实端点若未在响应中返回 `usage`，也会显示占位符。

**Q：Redis / PostgreSQL 没装会怎样？**
会自动降级：数据库回落 SQLite、短期记忆与队列回落进程内实现、长期记忆回落本地关键词检索。
`/api/health` 会如实报告每一项的实际状态。

---

## 6. 从命令行使用

```bash
python backend/cli.py "帮我分析 data/jds/jd03_数据分析师.txt，匹配简历并给面试题"   # 单轮
python backend/cli.py                                                            # 交互模式
```

---

## 7. 自测与排查

```bash
set PYTHONPATH=backend
python scripts/smoke_api.py     # 31 项端到端接口冒烟（含 SSE 事件序列与路径穿越防护）
python tests/test_smoke.py      # 77 项业务核心冒烟（离线）
```
