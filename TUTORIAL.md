# 求职智囊 Agent · 使用教程

> 面向日常使用的完整操作手册。部署与开发相关见 README.md。

---

## 一、启动前准备（仅首次）

```bash
cd "E:\ai\ai job\agent"
pip install -r requirements.txt
```

配置模型：把 `.env.example` 复制为 `.env`，填三项：

```ini
LLM_API_KEY=你的key
LLM_BASE_URL=https://api.generic-llm.com/v1     # 你的兼容端点
LLM_MODEL=generic-llm-chat                       # 默认模型
LLM_MODELS=generic-llm-chat,generic-llm-r1          # 可选：多模型切换列表
```

> 不配置 key 也能用：自动进入 mock 模式，流程完整可演示（分析结果为模板+真实工具计算）。

## 二、启动

| 方式 | 命令 | 适合 |
|---|---|---|
| Web 界面 | `python server.py` → 打开 http://127.0.0.1:8000 | 日常使用（推荐） |
| 命令行单次 | `python main.py "分析这份JD：…"` | 快速出结果 |
| 命令行交互 | `python main.py` | 终端多轮对话 |

改了 `.env`（模型/key/端点）后**必须重启服务**才生效；界面上切换模型（顶栏下拉）则即时生效。

## 三、界面导览

```
┌─ 顶栏：☰折叠侧栏 · 模型下拉 ⚡厂商 · 会话ID
├─ 左侧抽屉（三 Tab，可折叠成图标条）：
│   ⚡ 工作流 —— 目标/进度条/任务卡（点卡片看思考与工具调用）
│   🧠 记忆   —— 画像标签（岗位/城市/薪资…）+ 调用统计 + 清除记忆
│   📄 简历库 —— 拖拽上传 / 多份管理 / 点「使用」切换
└─ 主区：对话流（居中阅读列）+ 底部固定输入栏 + 🗑清空聊天记录
```

快捷键：`Enter` 发送 · `Shift+Enter` 换行 · `Ctrl+V` 粘贴截图 · `Esc` 关抽屉。

## 四、核心场景

### 场景 1：JD 截图分析（最快路径）

1. 在招聘网站截图 JD（含「岗位职责/任职要求」区域，尽量清晰）；
2. 回到本页，直接 **Ctrl+V**；
3. 看到「🖼 正在识别截图中的JD文字…」→ 自动填入识别文本并开始分析；
4. 右侧/侧栏工作流实时跑：解析 → 结构化 → （有简历时）匹配评分 → 面试题 → 报告落盘；
5. 报告渲染完成后，下方浮现快捷按钮（优化简历 / 出面试题 / 薪资谈判 / 查看数据）。

> 也可以把截图文件直接**拖进对话区**，效果相同。

### 场景 2：简历库管理

1. 顶栏/侧栏打开「📄 简历库」Tab；
2. **拖入或点击上传**简历（pdf / docx / txt / md，≤10MB）；
3. 多份简历时，点某份的「使用」设为默认（绿色"✓使用中"）；
4. 未手动选择时，Agent 自动使用**最新上传**的一份；点 🗑 可删除。

> 简历文件也可以直接拖进对话区（文件名含"简历/resume/cv"自动入库）。

### 场景 3：JD + 简历完整分析

任选其一提供 JD（粘贴文本 / 截图 / 文件路径 / 链接），简历在库里即可：

```
帮我分析这份JD并匹配简历，给出面试题：岗位职责：… 任职要求：…
```

或引用文件：`分析 data/jds/jd03_数据分析师.txt，匹配我的简历`

产出报告包含：结论摘要 / JD 解读 / 匹配分与图表 / 逐条优化建议 / 分类面试题 / 行动清单。
报告同时落盘 `data/reports/`，其中的「🔍 交互式报告」可在浏览器打开，悬停技能查看差距详情。

### 场景 4：材料不全时的多轮补全

只说目标（如"我想找算法工程师的工作"）→ Agent 会**只问一个关键问题** → 你补充材料 → 自动重新规划并继续执行，无需重复上下文。

### 场景 5：跟踪与清理

- 「查看我的数据」：随时查看 Agent 记住了什么（30 天未更新的项会标 ⏳ 待确认）；
- 「删除我的所有记忆」：清空画像与上下文，重新开始；
- 界面按钮：对话区右上「🗑 清空聊天记录」（删消息与存档，保留记忆和简历）；记忆 Tab 的「清除记忆」（本地重置）。

## 五、模型切换

- **界面**：顶栏下拉选择 → 立即生效（对之后的消息），toast 确认；
- **配置**：`.env` 的 `LLM_MODELS=模型A,模型B,模型C`（同一端点）；
- **兜底**：主模型连续失败自动切 `LLM_FALLBACK_MODEL`，顶栏会提示"已降级"。

> 速度建议：复杂分析用强模型（v4-pro 等）；日常问答/演示切轻量模型更快。

## 六、隐私与安全

| 机制 | 说明 |
|---|---|
| 敏感拦截 | 消息含身份证/银行卡/密码验证码 → 立即提示并忽略，不落盘不记忆 |
| 加密存储 | 会话/简历/截图落盘即加密（Fernet）；`DATA_KEY` 环境变量可自定义密钥 |
| 数据控制权 | 「查看我的数据」/「删除我的所有记忆」随时可用；会话文件存本地 `data/sessions/` |
| 清空聊天 | 删除消息与服务端存档（保留记忆和简历库） |

## 七、常见问题

| 现象 | 处理 |
|---|---|
| 回复慢 | LLM 推理耗时为主；换轻量模型或减少单次要求。工作流面板可实时看卡在哪一步 |
| 模型切换不生效 | 确认模型名在该端点可用；`.env` 改动需重启；界面切换即时生效 |
| OCR 识别不准 | 截图放大清晰区域重试；识别后也可手动修改输入框文本再发送 |
| 提示"文件不存在" | 检查路径（相对路径基于项目根目录）；简历被删除时先在简历库补传 |
| 页面异常 | 强刷（Ctrl+F5）；仍异常可点对话区「清空聊天记录」或删 `data/sessions/` 后重启 |
| 端口占用 | `netstat -ano | findstr 8000` 找 PID 结束，或改 `server.py` 端口 |

## 八、测试与批量评测

```bash
python tests/test_smoke.py          # 64项冒烟（离线，含故障注入/OCR/加密）
python tests/batch_eval.py          # 10个JD批量评测（真实LLM，约10-30分钟）
python tests/batch_eval.py --mock   # 离线快速回归
```

批量评测产物：`data/reports/batch_report.md`（总览+对比表+洞察）、每岗位独立报告与匹配图。

## 九、部署上线（速览）

```bash
docker build -t ai-job-agent .
docker run -d -p 8000:8000 --env-file .env -v ./data:/app/data ai-job-agent
```

裸机：`uvicorn server:app --host 0.0.0.0 --port 8000`（前置 Nginx 做 TLS）。
